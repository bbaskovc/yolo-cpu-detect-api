# Test Fixtures

No binary image fixtures are included in the initial repository baseline.

When fixtures are added:

- use small, lawful, non-sensitive images;
- record provenance and purpose;
- avoid real customer/personally sensitive images;
- document expected behavior and output tolerance;
- never use fixtures to smuggle model weights, API keys, or production data into Git.
